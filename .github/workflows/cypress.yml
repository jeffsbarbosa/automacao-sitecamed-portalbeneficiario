name: Testes Automatizados - Site Camed e Portal de Serviços Área Logada

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

jobs:
  cypress-run:
    runs-on: self-hosted

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v4

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Instalar dependências
        run: npm ci

      - name: Criar pasta de relatórios
        run: mkdir -p cypress/reports

      - name: Testar acesso ao site
        shell: powershell
        run: |
          try {
            Invoke-WebRequest https://apps.camed.com.br/CamedSaudeServicos -UseBasicParsing
          } catch {
            Write-Host "Falha no acesso ao site"
          }

      - name: Rodar testes automatizados
        uses: cypress-io/github-action@v6
        continue-on-error: true
        with:
          browser: chrome
          headed: true
          spec: | 
            cypress/e2e/TESTES_HOMEPAGE/**/*.cy.js          
            

      - name: Gerar relatório HTML
        if: always()
        shell: powershell
        run: |
          $files = Get-ChildItem -Path "cypress/reports" -Filter "*.json" | Select-Object -ExpandProperty FullName

          if (-not $files -or $files.Count -eq 0) {
            Write-Error "Nenhum arquivo JSON encontrado!"
            exit 1
          }

          npx marge $files --reportDir cypress/reports --reportFilename report

      - name: Upload dos relatórios
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: relatorio-cypress
          path: |
            cypress/reports
            cypress/screenshots
            cypress/videos

  gerar-relatorio-final:
    needs: cypress-run
    if: always()
    runs-on: self-hosted

    env:
      TOTAL: 0
      PASSED: 0
      FAILED: 0
      SUCCESS_RATE: 0
      TABLE_HOME: ""
      TABLE_PORTAL: ""
      TABLE_PERFIL: ""
      RATE_HOME: ""
      RATE_PORTAL: ""
      RATE_PERFIL: ""

    steps:
      - name: Ajustar encoding UTF-8
        shell: powershell
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8

      - name: Baixar relatórios
        uses: actions/download-artifact@v4
        with:
          path: all-reports

      - name: Consolidar resultados
        shell: powershell
        run: |
          $files = Get-ChildItem -Path "all-reports" -Recurse -File -ErrorAction SilentlyContinue |
          Where-Object { $_.Extension -eq ".json" }

          if (-not $files -or $files.Count -eq 0) {
            Write-Error "Nenhum JSON encontrado em all-reports"
            exit 1
          }

          [int]$TOTAL = 0
          [int]$PASSED = 0
          [int]$FAILED = 0

          foreach ($file in $files) {
            try {
              $json = Get-Content $file.FullName -Raw | ConvertFrom-Json

              if ($json.stats) {
                $TOTAL += [int]$json.stats.tests
                $PASSED += [int]$json.stats.passes
                $FAILED += [int]$json.stats.failures
              }
            } catch {
              Write-Host "Ignorando arquivo inválido: $($file.FullName)"
            }
          }

          echo "TOTAL=$TOTAL" >> $env:GITHUB_ENV
          echo "PASSED=$PASSED" >> $env:GITHUB_ENV
          echo "FAILED=$FAILED" >> $env:GITHUB_ENV

      - name: Calcular métricas
        shell: powershell
        run: |
          $rate = if ($env:TOTAL -eq 0) {0} else {[math]::Round(($env:PASSED/$env:TOTAL)*100,2)}
          echo "SUCCESS_RATE=$rate" >> $env:GITHUB_ENV

      - name: Montar tabelas por módulo (via spec)
        shell: powershell
        run: |
      
         $homeFail=@();$homePass=@()
         $portalFail=@();$portalPass=@()
         $perfilFail=@();$perfilPass=@()

         $hT=0;$hP=0;$pT=0;$pP=0;$pfT=0;$pfP=0

         function Row($suite, $test) {

         $status = if ($test.state -eq "failed") { "FALHA" } else { "SUCESSO" }

         $erro = if ($test.state -eq "failed" -and $test.err.message) {
          ($test.err.message -split "`n")[0]
         } else { "-" }

        $color = if ($status -eq "SUCESSO") { "#2e7d32" } else { "#c62828" }
        $bg = if ($status -eq "SUCESSO") { "#e8f5e9" } else { "#ffebee" }

        return "<tr style='background:$bg'>
          <td>$suite</td>
          <td>$($test.title)</td>
          <td style='color:$color;font-weight:bold'>$status</td>
          <td>$($test.duration) ms</td>
          <td>$erro</td>
        </tr>"
      }

      $files = Get-ChildItem "all-reports" -Recurse -File -ErrorAction SilentlyContinue |
        Where-Object { $_.Extension -eq ".json" -and $_.Name -like "mochawesome*" }

      foreach ($f in $files) {

        try {
          $json = Get-Content $f.FullName -Raw -Encoding UTF8 | ConvertFrom-Json
        } catch {
          Write-Host "Ignorando JSON inválido: $($f.FullName)"
          continue
        }

        foreach ($r in $json.results) {

          $filePath = $r.file.ToLower()

          foreach ($s in $r.suites) {
            foreach ($t in $s.tests) {

              $row = Row $s.title $t

              if ($filePath -like "*testes_homepage*") {
                $hT++
                if ($t.state -eq "failed") { $homeFail += $row } else { $homePass += $row; $hP++ }
              }
              elseif ($filePath -like "*testes_portal_area_nao_logada*") {
                $pT++
                if ($t.state -eq "failed") { $portalFail += $row } else { $portalPass += $row; $pP++ }
              }
              elseif ($filePath -like "*testes_perfil_associado*") {
                $pfT++
                if ($t.state -eq "failed") { $perfilFail += $row } else { $perfilPass += $row; $pfP++ }
              }

            }
          }
        }
      }

      function Rate($p,$t){
        if($t -eq 0){0}else{[math]::Round(($p/$t)*100,2)}
      }

      echo "RATE_HOME=$(Rate $hP $hT)" >> $env:GITHUB_ENV
      echo "RATE_PORTAL=$(Rate $pP $pT)" >> $env:GITHUB_ENV
      echo "RATE_PERFIL=$(Rate $pfP $pfT)" >> $env:GITHUB_ENV

      function Table($fail,$pass){
        $rows = ($fail + $pass) -join ''
        "<table style='border-collapse:collapse;width:100%;font-size:13px;font-family:Arial'>
          <tr style='background:#212529;color:#ffffff'>
            <th>Suite</th>
            <th>Cenário</th>
            <th>Status</th>
            <th>Tempo</th>
            <th>Erro</th>
          </tr>
          $rows
        </table>"
      }

      $home = Table $homeFail $homePass
      $portal = Table $portalFail $portalPass
      $perfil = Table $perfilFail $perfilPass

      "TABLE_HOME<<EOF" >> $env:GITHUB_ENV
      $home >> $env:GITHUB_ENV
      "EOF" >> $env:GITHUB_ENV

      "TABLE_PORTAL<<EOF" >> $env:GITHUB_ENV
      $portal >> $env:GITHUB_ENV
      "EOF" >> $env:GITHUB_ENV

      "TABLE_PERFIL<<EOF" >> $env:GITHUB_ENV
      $perfil >> $env:GITHUB_ENV
      "EOF" >> $env:GITHUB_ENV

      - name: Enviar email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: correio.camed.com.br
          server_port: 587
          secure: false
          ignore_cert: true
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "Testes Automatizados - Site Camed e Portal do Beneficiário"
          to: ${{ secrets.EMAIL_TO }}
          from: Jefferson Barbosa <${{ secrets.EMAIL_USER }}>

          html_body: |
            <meta charset="UTF-8">

            <h2>🚀 Execução dos testes automatizados concluída</h2>

            <h3>📊 Resumo</h3>

            <table style="width:100%;border-collapse:collapse;text-align:center">
              <tr><th>Total</th><th>Aprovados</th><th>Falhas</th><th>Taxa</th></tr>
              <tr>
                <td>${{ env.TOTAL }}</td>
                <td style="color:#28a745">${{ env.PASSED }}</td>
                <td style="color:#dc3545">${{ env.FAILED }}</td>
                <td>${{ env.SUCCESS_RATE }}%</td>
              </tr>
            </table>

            <h3>🏠 Homepage (${{ env.RATE_HOME }}%)</h3>
            ${{ env.TABLE_HOME }}

            <h3>🌐 Portal (${{ env.RATE_PORTAL }}%)</h3>
            ${{ env.TABLE_PORTAL }}

            <h3>🔐 Perfil (${{ env.RATE_PERFIL }}%)</h3>
            ${{ env.TABLE_PERFIL }}

          attachments: |
            all-reports/report.html